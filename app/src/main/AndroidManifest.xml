<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="manuelduarte077.notes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Notes">
        <activity android:name="manuelduarte077.notes.ViewActivity"></activity>
        <activity
            android:name="manuelduarte077.notes.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Notes.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="manuelduarte077.notes.CreateActivity"></activity>
    </application>

</manifest>